# Calculator Agent

An offline, deterministic demonstration of the Axiom Agent loop:

```text
user -> model -> calculator tool -> observation -> model -> final answer
```

Run from the repository root:

```bash
pnpm --filter @axiom-agent/example-calculator-agent start
```
